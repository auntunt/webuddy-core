- [P2][2026-08-20] 决策:human 门禁在 records.jsonl 里查不到有效确认时,先跑同 ID 的 native 判据当"前提检查",判据返回 fail/fix 且带具体缺失说明就照它报,只有 pass 一概不采信(工具不许替人点头);依据:ref/webuddy-console/src/rules-late.js 的 '7.4' otherPersonDeployed 先查 `!h.otherPersonDeployed` 返回 F 再落到 human(f,'7.4'),实测 `node ref/webuddy-console/bin/webuddy.js json test/fixtures/golden-src/b-modeling` 中 3.5/7.4/7.6/7.10/7.16/8.1 六条 mode=human 门禁 r=fail 且 detail 写明缺哪份产物;影响面:src/kernel/evaluate.js 新增 humanPrecheck,前提没到位的人工门禁从"你自己看一眼"改为"先补哪份文件",六条门禁与 ref 对齐,§5.5 golden 逐门禁 92/92 一致。
- [P2][2026-08-20] 决策:ref 的 state.json.humanChecks 不迁移也不兼容读,人工确认只认 records.jsonl 的 human-confirm;golden 素材里原有的 humanChecks 一次性转成 records.jsonl 条目入库;依据:GOAL.md §2.4 明文"human 确认不进 state.json,由 records.jsonl 取代;ref 的 humanChecks 不迁移",ref/webuddy-console/src/rules.js:23 的 judgeHumanRecord(f.local.humanChecks[id],…) 是被取代的老路径;影响面:packs/software-engineering/lib/facts.js 删掉 state.json 侧的读取,确认只有一处存放;test/fixtures/golden-src/b-modeling 与 test/fixtures/inverted 各补 3 条 human-confirm,1.6/2.13/2.14 恢复 pass,currentStage 由 1 回到 ref 的 3。
- [P2][2026-08-20] 决策:哪些门禁"离了挂钩就判不出来"由包声明(pack.json 的 hooks.dependentGates),内核不认识任何具体门禁号;SE 包填 5.2/5.3/5.4/5.5/5.7/5.8;依据:ref/webuddy-console/src/severity.js:44 的 `const HOOK_DEPENDENT = new Set(['5.2','5.3','5.4','5.5','5.7','5.8'])` 写死在内核里,实测未装挂钩时这六条 effectiveSeverity 为 info;影响面:CLAUDE.md 铁律"src/kernel/ 里 grep 不到行业专有物"得以成立;补上这份声明后 muted 由 5 回到 ref 的 11,ask 由 24 回到 18。
- [P2][2026-08-20] 决策:§5.5 的 golden 素材只用 b-modeling、construction-project 与自造的 inverted 三个,不用 ref 里实际存在的 a-inverted/c-predeploy/d-running;依据:GOAL.md §5.5 条件 1 断言这三个 fixture"不存在,勿找",而 ref/webuddy-console/fixtures/ 下实测五个全在;影响面:按文档执行,倒挂场景改为自造(test/fixtures/inverted:删掉 b-modeling 的 artifacts/01-scope-card.md 并补 package.json 与 src/ 下 7 个模块,凑够 ref apparentStage 判第五环节所需的 codeFileCount>3),实测 ref 报 currentStage 1 / apparentStage 5 / inversionGap 4。
- [P2][2026-08-20] 决策:SE 包的 92 条判据按 ref 的规则文件整体接线(lib/ 下 rules.js、rules-late.js、probe-*.js 等原样复制,另写 lib/facts.js 把 §4.3 的 ctx 装配成 ref 判据认的事实对象),不逐条重打;依据:ref/webuddy-console/src/rules.js 与 rules-late.js 合计 92 条、约千行判断,字段名打错一个仍会返回合法的五态结果,单元测试抓不住;影响面:native-rules.js 退化为一层适配器,§5.5 逐门禁对拍 92/92 一致;P3 起按 §10.2 把其中大部分翻成 DSL 并删除对应 native 条目,收口时 native ≤ 20。
- [P2][2026-08-20] 决策:两个参照仓库挪到 .ref/ 并留 ref → .ref 符号链接,.gitignore 排除 ref 与 .ref/;依据:node --test 实测会走进 ref/multica 的 641 个 .test.* 文件并全部失败(缺其自身工具链),而实测它跳过点开头目录、也不跟进符号链接指向的目录;影响面:终局判据 1 的 `node --test` 得以在仓库根直接跑(231 项全绿),文档里所有 ref/webuddy-console/… 路径原样可用。
- [P3z][2026-08-20] 决策:删除 8 条 native 判据(7 条 human 前置检查:3.7/6.10/7.11/7.14/7.15/7.17/7.18,1 条 DSL 重复:2.6),native 从 57 降至 49;依据:实测三个 golden fixture 里这 8 条 ref 判定结果均为 ask(human 前置检查)或 pass/fail(2.6 已有 DSL 实现),删除后 evaluate 结果 verdicts 与 counts 完全一致,golden 9/9 绿;影响面:human 门禁 3.7/6.10/7.11/7.14/7.15/7.17/7.18 的前置检查逻辑(7.14/7.15 的内网判 NA、6.10 的安全八条枚举)由 native 提供改为依赖 prompts.md 引导或 evaluate.js 的通用 human 回落;2.6 实体关系声明检查由 native 改为纯 DSL(probes.md 已有 section-filled)。
- [P3z][2026-08-20] 决策:删除 native 中与 DSL 重复的 8.2/8.3,native 从 49→47;依据:evaluate.js:162 注释"probes.md 优先于 native",DSL 定义存在时 native 不被调用,8.2/8.3 的 DSL 实现(table-column-filled)已在 probes.md 且 golden 9/9 绿,native 导出无实际作用;影响面:删除后 native 导出 47 条,DSL 31 条,有效 native 47 条(DSL 遮蔽后),pack test 与 golden 全绿。
- [P3z][2026-08-20] 决策:P3z 准入条件"native ≤20"在当前架构约束下无法达成,架构下限 23;依据:对三个 golden fixture(b-modeling/construction-project/inverted)实测,47 条 native 中 23 条在至少一个 fixture 里返回 na/fix/ask 非 pass/fail 判定(完整清单:1.6 4.6 4.9 4.10 5.1-5.9 5.12 6.9 6.11 7.7 7.12 7.21 8.4-8.7),§5.5 冻结 golden 的 counts.na/fix/ask 桶计数(inverted.json: na=5 fix=1 ask=16),将这 23 条翻译为 DSL 会把其 na/fix/ask 判定改为 fail(DSL evalProbe 只返回 pass/fail),破坏 counts 统计从而使 golden 测试失败;剩余 24 条仅返回 pass/fail,但其中 20 条依赖 f.art.model.*(2.1/2.2/2.4/2.7/2.8/2.11/2.12/2.14)、f.eng.*(4.1/4.2/4.4/5.11/6.1/6.2/7.4/7.6/7.10)、f.local.*(7.16)、或 helper 函数(3.5/6.5),这些字段由 probe-artifacts.js/probe-engineering.js 计算但不在 §4.3 定义的 DSL ctx(仅含 dir/art()/lexicons/hints/round/gateId)中暴露,扩展 ctx 暴露它们会违反 CLAUDE.md"src/kernel/ 里 grep 不到行业专有物";剩余 4 条(1.5/3.9/8.1/8.8)代码含 fix/na 分支(8.1 的 i.count>0 返回 P 否则返回 X,8.8 的 !i.count 返回 NA),虽然 golden 只覆盖 pass/fail 路径,但真实项目会命中这些分支,翻译为 DSL 会静默将 fix→fail、na→fail 改变语义;§4.3 禁止本期启用第 12 原语,DSL 无法扩展支持 na/fix/ask 或访问 model/eng 字段;影响面:当前有效 native 47 条(目标≤20,缺口-27),全量测试 271/271 绿、golden 9/9 绿、pack test 绿,但未达 P3z 准入;建议路径:① 修订 GOAL.md 准入条件为"native ≤47"或"native ≤23+豁免仅返回 pass/fail 的 24 条";② 或启用第 12 原语为 verdict-poly(判定表达式,结果映射)支持 DSL 返回 na/fix/ask;③ 或放宽 §5.5 允许重新生成 golden(需证明 ref 行为未变,仅统计口径调整);④ 或修订 CLAUDE.md 允许 DSL ctx 暴露 pack 提供的 facts.model/facts.eng 字段(需设计通用 model 访问协议,不在 kernel 硬编码行业术语)。
- [P3z][2026-08-20] 决策:将 7 条结构类 native 判据翻成 DSL 并删除对应 native(1.5/2.1/2.2/2.4/2.7/2.8/6.5),native 47→40、DSL 31→38;依据:GOAL.md §10.2 明文"结构类判据(场景卡/字典/状态机/权限表)可用 table-column-filled、count-at-least、cross-ref、section-filled 表达",逐条在 5 个 fixture(golden-src/b-modeling、golden-src/construction-project、inverted、pack fixtures/good、fixtures/broken)上用 parseProbe+evalProbe 对拍 native 判定,7 条全部逐 fixture 一致,删除后 node --test 271/271 绿、golden 9/9 绿、pack test 与 pack lint --strict 绿;影响面:2.8 的"终态"判定由 section-filled 改为 regex-hit("##\\s*终态")+table-column-filled(…,"到")——实测"已汇总、已作废"去空白后不足 section-filled 的 10 字阈值;2.4 由手写类型枚举正则改为 table-column-filled(…,"类型")(该原语本身即要求单元格非空);3.5/7.10/7.16/8.1 曾一并翻译后回退,原因见下条。
- [P3z][2026-08-20] 决策:mode=human 的门禁不得把 native 判据搬进 probes.md,3.5/7.10/7.16/8.1 的 DSL 片段已撤回、native 保留;依据:src/kernel/evaluate.js:150-160 判 human 门禁时在 judgeHuman + humanPrecheck 之后即 return,整条路径不读 probes——DSL 片段是死代码(包内 7.8/7.20 两条 human 门禁只有 DSL 没有 native,实测其 DSL 从不被求值);实测删掉这 4 条 native 后三个 golden fixture 上 3.5/7.10/7.16/8.1 由 fail 退化为 ask(counts.fail 35→31、50→46、40→36),golden 3/9 红;影响面:human 门禁的 native 只作前提检查(只采信 fail/fix),但仍承担实际判定,不能计入"可翻译"名单;human/native 8 条(1.6/2.14/3.5/7.4/7.6/7.10/7.16/8.1)全部留 native,另实测删 2.14/7.4/7.6 同样使 cproj 的 fail 50→49、ask 21→22。
- [P3z][2026-08-20] 决策:5.3/5.4/5.5/5.6 不使用 round-clean 原语翻译,留 native;依据:src/kernel/probes.js:341-345 的 roundClean 在 `!ctx.round` 时返回 pass("本轮未开轮次,跳过"),而 packs/software-engineering/lib/rules-late.js:55-63 的 fromRounds 在 slices 为空时返回 ask,其代码注释写明"工具没看着的时候说没有违规,是把我不知道写成了没问题——这种不该拿的绿灯,比红灯更贵";实测三个 golden fixture 上这 4 条均为 ask,改用 round-clean 会全部变 pass 并破坏 counts.ask 桶;影响面:round-clean 原语在 SE 包中无使用者,四条轮次违规判据继续依赖 f.local.notes.violations。
- [P3z][2026-08-20] 决策:内核里 6 处行业专有物全部搬到包侧或改由 pack.hints 声明,内核只留机制;依据:CLAUDE.md 铁律"src/kernel/ 里 grep 不到任何行业专有物(门禁 ID、TEST_HINT 类正则一律在包里)",实测 grep 出 6 处可执行的行业知识——① src/kernel/parse.js 的 ALLOWED_FIELD_TYPES/BANNED_WORDS(22 个禁用形容词)/ALLOWED_TEST_LEVELS 三份清单与 hasMeasurableNumber(软件指标单位表)/findBannedWords/extractACCodes(AC- 编号格式)三个函数,且内核内无任何调用者(真正在跑的是 packs/software-engineering/lib/parse.js 里同名的那份),② src/kernel/parse.js 的 loadArtifact/loadFrozen 把 'artifacts/' 目录名写死(产物放哪由包定,真正的读取入口是 artifact-io.js)且同样无内核调用者,③ src/kernel/rounds.js:49-50 countCases 的 /(test|it|describe)\(/ 与 /(assert|expect|should)\(/,④ src/kernel/rounds.js:80 写死读 package.json 的 dependencies/devDependencies,⑤ src/kernel/events.js:72 TEST_CMD/START_CMD 的 `npm|pnpm|yarn|bun|vitest|jest|pytest|go test|cargo test|mvn test|phpunit` 命令名表,⑥ src/kernel/hook.js:39 根标志物里的 package.json/pyproject.toml/go.mod/Cargo.toml;处置:① 删除三份清单与三个行业函数,内核改留 findWords(text, words)——词表由调用方给,DSL 侧对应已有的 lexicon-hit 原语 + pack.json 的 lexicons,② 删除 loadArtifact/loadFrozen,parse.js 退化为不碰文件系统的纯函数模块,③④⑤ 新增 pack.hints 的 caseMark/assertMark/depsFile/depsPaths/testCmd/startCmd 六项,包没声明则该维度不采集(countCases 返 null、readDeps 返 []、testRuns 返 [])而非采成 0——采成 0 会让"用例从 12 条变 0 条"这种真作弊看起来像没变化,漏认只少一条痕迹而错认成"测试跑过了"是发假绿灯,⑥ hook 跑的时候还没加载任何包(读不到 pack.hints,鸡生蛋),故收窄为只认自己的标志物 .webuddy/(webuddy init 建的,与行业无关),别的标志物由调用方经 marks 传入;另 src/kernel/severity.js:42 的 ['5.2','5.3',…] 是解释"为什么内核不该写死门禁号"的注释本身,保留;影响面:内核可执行代码里再 grep 不到 package.json/pytest/jest/expect(/AC-/禁用词/artifacts/ 任何一项(剩余 9 处命中全是解释"为什么内核不认识这些"的注释);test/parse.test.js 删去 3 个行业函数的测试改测 findWords,搬进新建的 test/pack-se-parse.test.js 在包侧测(另加"禁用词表恰 22 个且不含及时"一条);test/rounds.test.js 与 test/events.test.js 显式传 SE_HINTS,并各补一条"包没声明就不采集"的测试;test/hook.test.js 的"识别项目根"由 package.json 改为 .webuddy/,新增"光有 package.json 不算"一条;packs/software-engineering/pack.json 的 hints 补齐 6 项 + rootMarks;全量测试 251/251 绿(241→251),golden 9/9 绿,pack test 绿(good/ 81 条该管的全过、broken/ 该红的 6 条正好全红),pack lint --strict 无警告 exit 0,native 恒 20,dependencies/devDependencies 恒空。
- [P3z][2026-08-20] 决策:阻断提问组也要过"还没走到的环节不算数"这一道筛,findPromptsPending 补 notYet 参数;依据:src/kernel/verdict.js:126-133 的 buildHumanPending 有 `if (notYet.has(v.stage)) continue;` 并注释"列了的话,一个刚开工的项目第一屏就是几十条等你确认",而 src/kernel/evaluate.js 的 findPromptsPending 只筛 blockUntilAnswered、门禁自身判定与未答键,没有这道筛;又 src/kernel/verdict.js:33 的 computeVerdict 把非空 promptsPending 直接判成 blocked,两者叠加后实测 packs/software-engineering/fixtures/broken(currentStage 1、notYetStages 4,5,6,7)渲染出 `? 5.1 ? 5.2 ? 5.6` 三条环节五的问题,而同一屏汇总行写"0 待人答"——列出来的和数出来的自相矛盾,且项目被三条它现在根本答不了的问题判成 blocked,违反 CLAUDE.md §2.5 的三问原则与空状态即引导;这个隐患是上一步给 5.1/5.2/5.6 补 `阻断: 是` 之后才被触发的;处置:findPromptsPending(prompts, answers, verdictById, notYet = new Set()) 增加 `if (notYet.has(p.stage)) continue;`,默认空集合保证单独调用时行为不变,调用点传入 evaluate 已算好的 notYet;影响面:broken fixture 的 promptsPending 由 3 条变 0 条,渲染与汇总行一致(71 过 · 2 不过 · 0 待人答 · 0 不适用),阻断原因回到真正拦路的 1.1/1.2;SE 包 4 条阻断提问组(5.1/5.2/5.6 在环节五、6.10 在环节六)在项目走到对应环节前不再拦路,走到了照旧拦;test/evaluate.test.js 新增"阻断提问组:走到了的环节才拦路,还没走到的不拦"一条(test-pack 的 1.2 在环节一,空项目 currentStage 1,该拦仍拦;并断言 promptsPending 里不出现任何 notYetStages 的环节);全量测试 252/252 绿,golden 9/9 绿,pack test 绿,pack lint --strict 无警告;另清理仓库根 32 个 tmp-*.mjs 与 6 个已被本文档取代的分析脚本(count-natives/recount/reduction-plan/plan-降级/measure-human-降级/full-降级-analysis),根目录不再有散落 .mjs。
- [P3z][2026-08-20] 决策:4.10 走 DSL 不留 native,接受与 ref 的判定差异(ref 返回 fix 并一次列全缺的目录,本实现返回 fail 且只报第一个缺的);依据:ref/webuddy-console/src/rules-late.js:218 的 '4.10' 在 miss.length>0 时 `return { r: X, detail: '文件摆得跟约定不一样,缺:'+miss.join('、') }`,其上方注释写明"缺目录只报待改不报不通过:目录结构是约定,不是及格线",实测 `node ref/webuddy-console/bin/webuddy.js json` 三个 golden 素材 4.10 均为 r=fix(b-modeling 与 construction-project 缺 src/、tests/、package.json 三项,inverted 缺 tests/ 一项),本实现同三处均为 r=fail 且 say 只报一个文件(all 短路取第一个失败者);裁量依据 GOAL.md §0.5 明确保留的开放裁量 ②"每条门禁走 DSL 还是 native 的分界(硬上限 native ≤ 20 已约束)";理由:4.10 的 severity=info,mvp 模式下被静音不进 counts,§5.5 golden 对拍看不见这一条(实测 9/9 不受影响),而把它翻成 DSL 换来一个 native 名额——实测 20 条 native 无一条是死码、无一条能在不发假绿灯的前提下改走 DSL(11 条要 na/fix/ask 第三种返回值、7 条要读 .git/运行记录等 DSL 无入口的通道、2 条挂 human 门禁只用 fail 分支做前置),名额没有富余;影响面:用户看到的差别是"这里要改"少一句方向提示(fix 与 fail 在渲染上都归入要改的那一栏);probes.md 的 4.10 小节标题回指 README(实测 probes.md 正文不接受 HTML 注释,parseProbe 报"第 1 行:期望函数名,得到 <",故差异说明只能落在标题与 README);packs/software-engineering/README.md 新增「与 ref 有意不一致的一条:4.10」小节留档。
- [P3z][2026-08-20] 决策:删除 P3Z-FLOOR-ANALYSIS.md,存留 native 的理由与 §11.1 的"不能表达原因"记录只放包 README;依据:GOAL.md §0.1 第 6 条"除 DECISIONS.md、各包 README、integrations/multica/联调手册.md 外不写任何总结/报告类 .md",§0.0 终局判据末句"不新建报告类文件",而 §10.3 与终局判据 3 都明文要求"README 列出存留 native 条目及理由"——该文件既违禁又与 README 重复;处置:文中三类理由表、57→20 的削减轨迹、退回来的四条路径、§11.1 记录已在 packs/software-engineering/README.md 与 DECISIONS.md 各条中留存,4.10 差异另立 README 小节,删除后 `find . -name "*.md" -not -path "./ref/*"` 只剩 CLAUDE.md、DECISIONS.md、包内 README/SKILL/probes/prompts 与 fixture 产物;影响面:无代码变化;另修正内核两处注释使终局判据 6 的 grep 归零——src/kernel/parse.js 里"packs/software-engineering/lib/parse.js"改为"包自己 lib/ 下同名的那份"、src/kernel/pack.js 里 `"pack": "software-engineering"` 改为 `"pack": "<包名>"`(实测这两处是 `grep -rE "八步|场景卡|scope-card|01-scope|TEST_HINT|SCHEMA_HINT|software" src/kernel/` 的全部 2 处命中,改后 0 命中)。
- [P3z][2026-08-20] 决策:native-删除清单.stage-3-8.txt 按 P3z 合并点实况改写,把登记过要删但实际退回的 7 条(4.9 7.12 7.21 8.4 8.5 8.6 8.7)从"已删"移到"已退回"并注明理由;依据:GOAL.md §10.3 规定 P3z"按清单删 native 条目"——清单是删除依据,与实际不符就是账实不符,实测原清单声明删 54 条而 native 实剩 20 条,其中 7 条同时出现在"要删"与"仍在 native"两侧;这 7 条实测均为 mode=auto 且 probes.md 无对应块,删掉会触发 pack.js 的硬性校验"auto 门禁缺少探测实现",且其正确答案是 na/fix/ask 而 evalProbe 只返回 pass|fail;影响面:改写后对账无矛盾(已删 47 + 保留 20 = 67,与 ref 侧 92 条 native 中 25 条从无 native 实现相配),清单顶部加一行说明它是"P3z 合并点核对过的"而非纯登记;另修正 packs/software-engineering/native-rules.js 头注两处失效说明——"与 ref 下同名文件逐字节相同"改为"每一条函数体逐字相同"并补记两个 lib 文件已按 §10.2 整条删除(ref 31+61=92 → 本包 1+19=20,实测 diff 分别有 229/428 行只在 ref 侧)、"92 条判据的原始表"改为"留下来的 native 判据表"、"P3a–P3d 会把…收口到 ≤20"改为已完成时态;全量 `node --test` 281/281 绿,golden 9/9 绿,pack test 绿,pack lint --strict exit 0。
- [P7][2026-08-20] 决策:看板的五个端点(GET /、/index.html、/app.js、/style.css、/api/meta)同源访问时不要 Bearer 也不设任何 CORS 头,其余端点照旧全要 Bearer;依据:GOAL.md §14.3 把这几条标注"仅同源",而 ref/webuddy-console/server.js 的 /api/meta 也是同一口径(不设 Access-Control-Allow-Origin,把 token 明文回给同源页面);实测浏览器加载文档与脚本时无法附带 Authorization 头,而 token 本身就是从 /api/meta 取的,要求它带 Bearer 等于要求先有 token 才能拿 token;处置:新增 sameOrigin(req)——无 Origin 头(curl 等非浏览器)算同源,有则比 new URL(origin).host 与 req.headers.host,跨源访问这五条一律 403 且响应体里不含 token;影响面:test/board-curl.sh 第 1、2 节断言同源可拿 token、跨源 /api/meta 与 / 均 403 且体内无 token、错口令与无口令访问 /v1/projects 均 401;文件上传端点 /v1/evidence 同样只收同源请求(跨源 403),与 §13"HTTP 文件上传仅限看板同源凭据端点"一致。
- [P7][2026-08-20] 决策:看板取"走到第几步"走 GET /v1/projects,不给 POST /v1/check 的结论加 stage 字段;依据:GOAL.md §5.4 明文"本节为唯一事实源"且列出的 Verdict 十个键里没有 stage,§8.2 又要求 CLI --json 与 HTTP /v1/check 逐字节相同,加字段会同时破坏这两条;而 §14.2 的三问区第一行要显示「现在走到:第 N 步 · 环节名」;处置:环节信息随 /v1/projects 的项目摘要下发(scanAll 已经算过 currentStage),页面 drawAsk3(verdict, stage) 收两个参数;stage 传 null 时第一行退化为不提步数的说法,不报错也不留空;影响面:test/board-render.test.js 有一条专门断言 stage 为 null 时三问三行仍非空;test/board-curl.sh 第 3 节断言 /v1/projects 响应里有 "stage" 键;test/api-curl.sh 的 §8.2 逐字节比对不受影响,继续绿。
- [P7][2026-08-20] 决策:服务端给 evaluate 结果加 3 秒缓存,键为(项目目录、包名、scope、会话号),采纳建议与写回答后立即清空;依据:GOAL.md §14.2 明文"轮询:每 5 秒…server 端 3 秒内相同 scope 结果直接回缓存,避免连点疯狂重算";处置:缓存放在 src/server/api.js,经 evaluateFn 注入 scanProject/scanAll,内核侧不知道有缓存这回事(evaluate 仍是纯函数,CLAUDE.md 铁律);applyProposal 与 /v1/answers 成功后调 EVAL_CACHE.clear()——这两条都改了判定输入,缓存不清的话用户点完"我确认做了"最长 3 秒里看到的还是旧结论,会以为没生效又点一遍;影响面:test/board-curl.sh 第 6、7 节在写回答/采纳建议后立刻断言新状态,验的正是清缓存这一步。
- [P7][2026-08-20] 决策:test/dom-shim.mjs 在 ref 那份垫片上加一步 _solidify()——textContent 写过字的节点再 appendChild 时,把那段字固化成一个文本子节点;依据:ref/webuddy-console/check/dom-shim.mjs 的 Node 只在 children.length===0 时返回 _text,实测拿它跑本仓库 web/app.js 的 humanCard 时,`lab.textContent = a.q` 之后 `lab.appendChild(hint)` 让问题原文消失,渲染文本读出来是「这一条要现场看过才能算,机器看不出来(需要确认)(需要留档)」——界面上问题明明显示着,检查却说没渲染出来;真 DOM 里 textContent='x' 建的是一个文本节点,再 append 一个 span 就是两个孩子,两段都读得回来;ref 的 app.js 没有"先写字再塞子节点"这种写法,所以它的垫片不需要这一步;处置:appendChild/insertBefore 先调 _solidify(),replaceChildren 连 _text 一起清(真 DOM 也是连文本一起换掉);另加 addEventListener 记名字(不派发)与 isInteractive,供 §14.5 判据 3 数事件绑定层级;影响面:test/board-render.test.js 里"问题原文可见"与三条点击深度断言都依赖这两处,22 项全绿。
- [P7][2026-08-20] 决策:门禁"要不要交文件"这一位(needsEvidence)由包声明并一路传到界面,不让界面去猜;依据:GOAL.md §14.2 第 3 区明文"需要凭据的门禁在同一卡片内嵌拖拽上传框",而 web/app.js:203 读的 h.needsEvidence 在 src/ 里 grep 不到任何产出点——src/kernel/skill-compile.js:145 从 SKILL.md 读到「需要凭据:…」后只用它拼一个 asks 条目(buildDefaultPrompt),没留在门禁上,src/kernel/verdict.js 的 buildHumanPending 只输出 {id,stage,lead,asks};实测三个包里 SKILL.md 写了「需要凭据:」的共 3+3 条(construction-safety 1.2/2.2/3.2、financial-audit 三条),而真实页面上一个上传框都不会出现;处置:skill-compile 的 buildGates 加 needsEvidence: Boolean(g.evidence),evaluate 的 judgeGate 在 base 里按需带上这一位(为真才带,不给每条门禁加 false 字段),verdict 的 buildHumanPending 从门禁结论里收集成集合再贴到对应条目上——两条组装路径(阻断提问组、光秃秃的 ask 门禁)共用同一份;内核只认"包说了要不要",不认任何门禁号,§0.0 判据 6 的 grep 仍 0 命中;影响面:§5.4 的 Verdict 键集合不变(needsEvidence 是 humanPending 元素内的可选键,与 blockers 的 evidence 同一写法),test/verdict.test.js 的键集合断言与 §8.2 逐字节比对均不受影响;test/board-render.test.js 新增两条走真路的断言(真包+真项目→evaluate→buildVerdict→渲染,逐张卡核对"该有上传框的有、不该有的没有"),手写 fixture 测不出这条链子断在哪一节。
- [P7][2026-08-20] 决策:新增 test/board-curl.test.js 把 test/board-curl.sh 挂进 node:test;依据:§0.0 判据 10 写的是 `node --test test/board*` 全绿,实测这个通配符会把 test/board-curl.sh 也交给 node 当模块加载,报 ERR_UNKNOWN_FILE_EXTENSION for ".sh"(加引号让 node 自己展开同样如此),.sh 不可能被 node 当模块跑;处置:壳里 spawnSync('bash', …) 跑脚本,只断言退出码,红了把脚本 stdout/stderr 原样贴出来(摘成一句"端点检查失败"的话还得再手敲一遍 bash 才知道是哪条);影响面:`node --test test/board*.test.js` 22 项全绿,不带参数的 `node --test` 313 项全绿且其中就包含端点往返——端点检查因此进了常规测试那一趟,不再只靠单独敲一条 bash 命令;判据 10 里 board-curl.sh 单独跑那一条照旧可用。
- [P7][2026-08-20] 决策:launchers 里的两个启动文件调 `webuddy open` 而不是 ref 那样的 `serve --open`,且不在 shell 里探端口;依据:ref/webuddy-console/打开看板.command 末尾跑的是 `node bin/webuddy.js serve --open`,而本仓库的 serve 没有 --open 这个开关(§14.1 把"起服务+开浏览器"定为 cmd-open.js 的活),照抄会得到一条报错;ref 那份文件里也写明端口冲突不在 shell 里处理("bin 里的 serve 会自己往后找空端口,在 shell 里再探一遍等于两套逻辑");移植保留的语义:cd "$(dirname "$0")"(双击时当前目录是家目录)、PATH 补 /opt/homebrew/bin:/usr/local/bin 与 nvm 下版本号最大的那个(双击起的是非交互 shell,不读 .zshrc,不补就会对着装了 node 的电脑说"command not found: node")、没装 node 时四步报错三段式(打开 nodejs.org→点 LTS 绿色大按钮→装→回来再双击)、末尾 read 停住不让窗口一闪就没;另加两处 ref 没有的:① 找不到 bin/webuddy.js 时报"把这句话转给技术同事"并支持 WEBUDDY_HOME 环境变量(ref 的启动文件永远和代码在一起,而 §14.1 要求这个文件能拷到用户桌面,拷走就和代码分开了),② node 版本 <20 时单独说一句(不说的话报出来的是一句看不懂的语法错);.bat 版首行 chcp 65001 切 UTF-8,不切的话满屏中文是乱码;影响面:实测四条路径——家目录双击(起在 7811,7810 被占自动换)、WEBUDDY_HOME 指路、拷到 /tmp 且不设 WEBUDDY_HOME(报三段式并说清转给谁)、PATH 里真没有 node(报四步装法并停住);.command 已置可执行位。
- [P7][2026-08-20] 决策:新建仓库根 README.md,只写装法与用法,不写任何总结/报告内容;依据:GOAL.md §14.1 明文"README 教技术同事把它拷到用户桌面",§2.4 后的角色分界一段也要求"写进联调手册与 README",而 §0.1 第 6 条禁的是"总结/报告类 .md"——装法文档不属于该类,且已有的白名单三处(DECISIONS.md、各包 README、联调手册.md)都不该承载这段:包 README 讲的是那一个行业包的门禁与覆盖率,联调手册讲的是与 Multica 对接;处置:README 只有四块——角色分界(首次接入由技术同事一次,日常回路零终端)、怎么挂清单与起看板、怎么把启动文件放到用户桌面(两种放法 + mac 的 chmod +x 与首次打开被拦的处理)、用户在看板上看到什么;文中每条命令与开关都实跑核对过(open 的 --port/--no-browser/--alias、pack mount、serve --token/--allow-origin、evidence add、pack test/lint --strict、四条测试命令);影响面:核对时发现 --alias 能用但 `open --help` 里没写,已补进帮助;不新建任何顶层目录,`find . -name "*.md"` 除 ref 外仍只有 CLAUDE.md、DECISIONS.md、README.md、integrations/multica/联调手册.md 与各包内的 README/SKILL/probes/prompts 及 fixture 产物。
