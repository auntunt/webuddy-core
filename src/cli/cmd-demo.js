/**
 * GOAL-2 I0 存根：命令已在 bin/webuddy.js 注册，实现留给后续任务（I3a/I3b/I4a）。
 * 存根期照 bin 原来的口径回话：打印「尚未实现」并 exit 2。
 */
export async function run(_positionals, _flags) {
  console.error('尚未实现');
  process.exit(2);
}
